# Contributing

Open an issue before large changes. Pull requests must keep the UI usable at 960 × 480, avoid unnecessary dependencies, pass tests and lint, and must not log secrets or weaken TLS validation.

Use focused commits and include real-device test results for launcher, sensor, networking, or display-lifecycle changes.
